//arrays is a object in which we store collection of multitlpw items(datatypes) in single variables. 

// array element cant be accesed using arbitary.
const myarr = [0,1,2,3,4,5]

console.log(myarr[3]);

myarr.push(6)
console.log(myarr);